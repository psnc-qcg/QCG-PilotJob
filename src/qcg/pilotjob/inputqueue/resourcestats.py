
class ResourceStats:

    def __init__(self, total_cpus, total_nodes, max_node_cpu_cores):
        self.total_cpus = total_cpus
        self.total_nodes = total_nodes
        self.max_node_cpu_cores = max_node_cpu_cores
